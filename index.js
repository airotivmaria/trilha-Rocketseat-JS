// arrays, objetos 
let meta = {
    value: 'ler um livro por mês',
    checked: false,
    isChecked: (info) => {
        console.log(info)
    }
}

console.log(meta.value)
meta.isChecked()

//function // arrow function
let criarMeta = () => {}

// function criarMeta() {}
